import React from 'react';
import { Image, View } from 'react-native';
import { IMAGES } from '../images';

export const Carrot = () => {
  return <Image source={IMAGES.dropdownArrow} style={{ width: '100%', height: '100%' }}></Image>;
};
